# API URL for the spell checker backend (used in API mode)
VITE_API_URL=http://localhost:8080

# Build mode: 'wasm' for GitHub Pages deployment (no backend needed)
# 'api' for local development with backend server
# 'auto' to try WASM first then fall back to API
VITE_SPELLCHECKER_MODE=wasm
